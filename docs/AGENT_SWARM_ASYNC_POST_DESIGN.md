# Agent Swarm: Async Post Design Archive

This document is now archival.

The design rationale, specialist/tool decisions, and mermaid diagrams from this document were merged into [`AGENT_SWARM_CONTRACT.md`](AGENT_SWARM_CONTRACT.md), which is now the single source of truth for the agent-swarm architecture.

Use [`AGENT_SWARM_CONTRACT.md`](AGENT_SWARM_CONTRACT.md) for:

- current architecture
- async-post behavior
- stage-specific coordinator rules
- tool ownership
- mermaid diagrams

Keep this file only as a historical pointer so older links do not break.
